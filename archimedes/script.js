// a square + b square = c square

// get value of a from user and store it in variable a
a = prompt("provide a")

// get value of b from user and store it in variable b
b = prompt("provide b");

//square values
c = (a*2 + b*2) *2;

//print the results
console.log(`entered a: ${a} and entered b: ${b} and it's c = ${c}`);
